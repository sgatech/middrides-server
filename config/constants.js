const ROUTES = {
    INDEX: "/",
    SHUTDOWN: "/shutdown",
    TURNON: "/turnon"
};

const COLLECTIONS = {
    STATUS: "status"
};

const CONSTANTS = {
    ROUTES: ROUTES,
    COLLECTIONS: COLLECTIONS
};

module.exports = CONSTANTS;