const ROUTES = {
    INDEX: "/",
    SHUTDOWN: "/shutdown",
    TURNON: "/turnon",
    LOGIN: "/login",
    REGISTER: "/register",
    CHANGE_PASSWORD: "/changepwd"
};

const COLLECTION = {
    STATUS: "status",
    USER: "user"
};

const CONSTANTS = {
    ROUTES: ROUTES,
    COLLECTION: COLLECTION
};

module.exports = CONSTANTS;