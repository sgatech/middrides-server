const ROUTES = {
    INDEX: "/",
    SHUTDOWN: "/shutdown",
    TURNON: "/turnon",
    LOGIN: "/login",
    REGISTER: "/register"
};

const COLLECTION = {
    STATUS: "status",
    USER: "user"
};

const CONSTANTS = {
    ROUTES: ROUTES,
    COLLECTION: COLLECTION
};

module.exports = CONSTANTS;